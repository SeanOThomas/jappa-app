import 'package:flutter/widgets.dart';

class MedDetailPage extends StatelessWidget {
  static const ROUTE_NAME = '/med_detail';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("detail page")
    );
  }
}